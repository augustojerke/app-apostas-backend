generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "url"
}

model Usuario {
  id     Int     @id @default(autoincrement())
  nome   String  @unique
  senha  String
}
